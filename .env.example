# Clerk authentication (free Hobby plan) — https://dashboard.clerk.com
# Create an application, then copy the Publishable Key here.
# The publishable key is safe to expose in the frontend (it is not a secret).
# Without this key the app still runs, but auth features are disabled.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
